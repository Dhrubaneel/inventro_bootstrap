type InventroConfig 
  @model 
  @versioned
  @auth(rules: [{ allow: owner }, { allow: private }])
  @key(fields: ["pk", "sk"]) {
  pk: String! @primaryKey
  sk: String! @primaryKey(sortKeyFields: ["sk"])
  description: String
}
