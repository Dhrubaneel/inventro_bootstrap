{
  "name": "inventro-backend",
  "version": "1.0.0",
  "description": "Lambda code that works as Inventro API",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "lambda.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.782.0",
    "@aws-sdk/lib-dynamodb": "^3.782.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "jsonschema": "^1.5.0"
  }
}
